[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "workspace"
version = "0.1.0"
description = "Local filesystem workspace for A-Liang-UI (default ~/.a-liang-ui)"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["workspace"]
