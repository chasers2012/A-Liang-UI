[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "agent"
version = "0.1.0"
description = "Quant Agents"
requires-python = ">=3.10"
dependencies = [
    "langgraph",
]

[tool.hatch.build.targets.wheel]
packages = ["agent"]
