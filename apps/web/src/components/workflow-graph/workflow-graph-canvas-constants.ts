/** 左侧「添加节点」拖到画布时使用的 DataTransfer MIME（避免与普通文本拖放冲突）。 */
export const WORKFLOW_GRAPH_NODE_DRAG_MIME =
  "application/x-workflow-graph-node-type";

export const DEFAULT_FIT = { padding: 0.14, maxZoom: 1.15, minZoom: 0.08 };
