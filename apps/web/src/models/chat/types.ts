/** 前端聊天 UI（与 ``AgentAssistantBlockPublic`` / ``AgentChatToolCallPublic`` 对齐）。 */

export type {
  AgentAssistantBlockPublic as AssistantBlock,
  AgentChatToolCallPublic as ChatToolCallDisplay,
} from "../agent-llm/dto";
