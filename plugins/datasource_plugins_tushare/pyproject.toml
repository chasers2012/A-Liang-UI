[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "datasource-plugins-tushare"
version = "0.1.0"
description = "Tushare datasource plugin for quant-agent"
requires-python = ">=3.10"
dependencies = [
    "quant-agent-api",
    "tushare>=1.4.0",
    "tqdm>=4.66.0",
]

[project.entry-points."quant-agent.plugins"]
tushare = "datasource_plugins_tushare:TushareDataSourcePlugin"

[tool.uv.sources]
quant-agent-api = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["datasource_plugins_tushare"]
