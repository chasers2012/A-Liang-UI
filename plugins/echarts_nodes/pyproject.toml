[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "echarts-nodes"
version = "0.1.0"
description = "ECharts workflow node plugin for quant-agent"
requires-python = ">=3.10"
dependencies = [
    "quant-agent-api",
    "workflow",
    "echartsy>=0.7.4",
]

[project.entry-points."quant-agent.plugins"]
echarts = "echarts_nodes.plugin:EchartsNodesPlugin"

[tool.uv.sources]
quant-agent-api = { workspace = true }
workflow = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["echarts_nodes"]
