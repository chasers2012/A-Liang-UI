[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "technical-nodes"
version = "0.1.0"
description = "Technical indicator workflow node plugin for A-Liang-UI"
requires-python = ">=3.10"
dependencies = [
    "a-liang-ui-api",
    "workflow",
    "vectorbt",
]

[project.entry-points."a-liang-ui.plugins"]
technical-nodes = "technical_nodes.plugin:TechnicalNodesPlugin"

[tool.uv.sources]
a-liang-ui-api = { workspace = true }
workflow = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["technical_nodes"]
