[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "embedding-plugins-huggingface"
version = "0.1.0"
description = "HuggingFace embedding provider plugin for quant-agent"
requires-python = ">=3.10"
dependencies = [
    "quant-agent-api",
    "langchain-huggingface>=0.1.0",
    "sentence-transformers>=3.0.0",
]

[project.entry-points."quant-agent.plugins"]
embedding_huggingface = "embedding_plugins_huggingface:HuggingFaceEmbeddingPlugin"

[tool.uv.sources]
quant-agent-api = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["embedding_plugins_huggingface"]
