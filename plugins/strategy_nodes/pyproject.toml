[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "strategy-nodes"
version = "0.1.0"
description = "Strategy and backtest workflow nodes for quant-agent"
requires-python = ">=3.10"
dependencies = [
    "quant-agent-api",
    "factor",
    "workflow",
]

[project.entry-points."quant-agent.plugins"]
strategy = "strategy_nodes.plugin:StrategyNodesPlugin"

[tool.uv.sources]
quant-agent-api = { workspace = true }
factor = { workspace = true }
workflow = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["strategy_nodes"]
